# Database (SQLite for quick start; switch to PostgreSQL/MySQL in production)
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET="supersecret-change-me"
JWT_EXPIRES_IN="7d"

# App
APP_URL="http://localhost:4000"
PORT=4000

# Mail (Option A: real SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=app_password
SMTP_FROM="Your App <no-reply@yourapp.com>"

# Mail (Option B: Ethereal for dev – leave empty to auto-create)
USE_ETHEREAL=true
